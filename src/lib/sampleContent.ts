export const sampleContent = `# Welcome to Markzen

Markzen is a fast, beautiful, and focused Markdown editor. Start typing to see **inline formatting** in action.

## Features

- **Bold**, *italic*, and \`inline code\` render as you type
- Headings convert automatically when you prefix with \`#\`
- Lists, blockquotes, and code blocks just work

## Code Example

\`\`\`typescript
function greet(name: string): string {
  return \`Hello, \${name}!\`;
}
\`\`\`

> "Simplicity is the ultimate sophistication." — Leonardo da Vinci

---

Try editing this content. Everything you see is **live Markdown** rendered as rich text.
`;
