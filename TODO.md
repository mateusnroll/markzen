# TODO

- [ ] Remove `src/lib/sampleContent.ts` once the mocked data is no longer needed
- [ ] Remove or customize the window `title` in `src-tauri/tauri.conf.json` — removing it didn't work as expected
